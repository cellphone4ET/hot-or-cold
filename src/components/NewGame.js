import React from 'react';

export default class NewGame extends React.Component {
  constructor(props) {
    super(props);
  }

  // newGame(e) {
  //
  // }

  render() {
    return (
      <button>New Game</button>);
  }
}
